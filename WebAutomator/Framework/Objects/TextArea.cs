using OpenQA.Selenium;

namespace Framework.Objects
{
    public class TextArea : Control
    {
        public TextArea(IWebElement element, string name) : base(element, name)
        {
        }
    }
}
